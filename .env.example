# LIFF app id จาก LINE Developers console (LINE Login channel -> LIFF)
VITE_LIFF_ID=

# Base URL ของ API จริง — เว้นว่างไว้เพื่อใช้ mock API (MSW)
VITE_API_URL=

# เปิด/ปิด mock API ด้วยมือ ("true" | "false") ถ้าไม่ตั้งจะเปิดอัตโนมัติเมื่อไม่มี VITE_API_URL
VITE_ENABLE_MOCK=
